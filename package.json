{
  "name": "moneymind-mcp",
  "version": "1.0.0",
  "description": "MCP server for MoneyMind — Plaid transaction sync and financial analysis",
  "main": "server.js",
  "scripts": {
    "start": "cd web && npm install && node app.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.78.0",
    "@modelcontextprotocol/sdk": "^1.0.0",
    "dotenv": "^16.0.0",
    "plaid": "^17.0.0"
  }
}
